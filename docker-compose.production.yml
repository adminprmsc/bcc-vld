# Production Linux VM — host build (deploy/production.sh); no npm in Docker
services:
  backend:
    build:
      dockerfile: Dockerfile.production

  frontend:
    build:
      dockerfile: Dockerfile.local
